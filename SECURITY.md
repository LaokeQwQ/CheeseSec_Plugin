# 安全策略

## 报告问题

请通过私下渠道向维护者报告漏洞，不要在公开 Issue 中披露未修复的密钥、Token、客户数据或可利用细节。报告应包含影响范围、复现步骤、受影响版本和安全联系方式；维护者确认后再安排公开披露。

## 密钥和签名

私钥、wrapping key、恢复凭证和 Token 永远不入库，也不得出现在 CRP、目录、OTA 索引、日志或截图中。签名根应离线或由 KMS 管理，按官方、企业、社区、个人、测试和开发来源分开授权，并维护轮换与吊销记录。

目录条目不是授权。CheeseWAF 必须在安装前重新验证签名阈值、来源根、摘要、版本序号、权限声明和吊销状态。

## 高风险操作

不可信、未知、测试或开发签名不得静默启用高权限。允许继续时，管理员必须阅读风险告警并等待 10 秒，再输入密码完成二次和三次确认；结果写入不可抵赖的审计记录，并提供回滚方案。

## 断网与供应链

离线模式不得主动连接外网。在线更新必须由管理员密码确认并记录连接信息。CWEDP 传输支持断点续传，完成后校验 MD5、SHA-1 和 SHA-256；任何校验失败都必须拒绝激活。发布、镜像和 OTA 服务不得改写包的来源、摘要或签名。

安全问题修复前，不要把临时绕过方案写成正式能力。
